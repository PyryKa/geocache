import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <h1>
        Hover markers for more information. Click on Helsinki area (green) to
        add new markers.
      </h1>
    );
  }
}

export default Header;
